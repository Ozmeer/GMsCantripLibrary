[h: isGmClient = isGM()]
[h, if(isGmClient): 
  broadcast("GMs macros library installed. Create the "Campain macros" button and execute [macro(\"center_broadcast@net.kiktaaa.gmsmacrolib\")].", "gm")
]